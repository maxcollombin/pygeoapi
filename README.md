# pygeoapi

Basic pygeoapi configuration for publishing OGC API Processes.

1. Start the container

```docker
docker compose up
```

2. Access the service via the following url:


`http://localhost:5000/`
